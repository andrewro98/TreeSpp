/*

  dense_bst.cpp
  Andrew Rosen Dec 2018
  CPP file for dense binary search tree.

*/

#include "dense_bst.h"

template <typename T>
virtual bool dense_bst::insert(T & obj)
{
  return false;
}

template <typename T>
virtual bool dense_bst::contains(T & obj)
{
  return false;
}

template <typename T>
virtual T dense_bst::find(T & obj)
{
  return obj;
}

template <typename T>
virtual bool dense_bst::erase(T & obj)
{
  return false;
}
